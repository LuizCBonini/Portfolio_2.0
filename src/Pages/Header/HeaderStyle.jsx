import styled from "styled-components";

export const HeaderStyle = styled.div`
    >div {
        display: flex;
        
        .nav-bar > .menu {
            list-style: none;

            li a span {
                color: var(--cll-primary);
            }
        }
    }
    
`;